{
  "name": "jacobmgevans",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev --experimental-json-config",
    "deploy": "wrangler deploy src/index.ts --experimental-json-config",
    "update": "rm -rf pnpm-lock.yaml node_modules && pnpm i"
  },
  "dependencies": {
    "wrangler": "^4.11.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250416.0",
    "typescript": "^5.8.3"
  }
}
