{
  "name": "jacobmgevans",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "deploy": "npm run build && wrangler deploy",
    "update": "rm -rf pnpm-lock.yaml node_modules && pnpm i",
    "scan": "pnpm dev & npx react-scan@latest localhost:3000"
  },
  "dependencies": {
    "hono": "^4.7.7",
    "tailwindcss": "^4.1.4",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.4",
    "@tailwindcss/postcss": "^4.1.4",
    "@tailwindcss/cli": "^4.1.4",
    "@cloudflare/vite-plugin": "^1.0.13",
    "@cloudflare/workers-types": "^4.20250417.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.3",
    "wrangler": "^4.12.0"
  }
}
